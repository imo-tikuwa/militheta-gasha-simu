$(() => {

    // ガシャID
    $('#gasha-id').select2({
        theme: "bootstrap-5",
        width: 'auto',
        dropdownAutoWidth: true,
    });

    // カードID
    $('#card-id').select2({
        theme: "bootstrap-5",
        width: 'auto',
        dropdownAutoWidth: true,
    });

});
