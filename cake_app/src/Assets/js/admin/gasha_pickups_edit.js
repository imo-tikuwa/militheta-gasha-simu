$(function(){

	// ガシャID
	$('#gasha_id').select2({
		theme: "bootstrap4",
		width: 'auto',
		dropdownAutoWidth: true,
	});

	// カードID
	$('#card_id').select2({
		theme: "bootstrap4",
		width: 'auto',
		dropdownAutoWidth: true,
	});

});
